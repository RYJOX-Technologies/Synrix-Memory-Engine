# Local embedding models (default, no API key needed)
sentence-transformers>=2.2.0
torch>=2.0.0

# Cloud embedding APIs (optional, requires API keys)
openai>=1.0.0
cohere>=4.0.0

# Core dependencies
numpy>=1.24.0
